rows = int(input("enter the number of rows : "))
num = float(input("enter the number : "))
for i in range(rows):
    for j in range(i + 1):
        print(f"{num:.1f}", end=" ")
        num += 0.1
    print()
